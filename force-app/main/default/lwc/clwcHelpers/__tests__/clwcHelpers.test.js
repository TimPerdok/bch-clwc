import { validateStyleProperties } from "../clwcHelpers";

function format(content) {
    return content.toLowerCase().replace(/\s/g, "");
}

describe("CSS Formatter", () => {
    afterEach(() => {
        jest.clearAllMocks();
    });


});
